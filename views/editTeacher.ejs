<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Tuition Centre App</title>
</head>

<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Tuition Centre App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/registerT">Add new teacher</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center">
      <h2>Edit Teacher Info</h2>
    </div>
    <br>
    <div class="text-center">
      <% if (teacher) { %>
        <form action="/editTeacher/<%= teacher.teacherId %>" method="POST" enctype="multipart/form-data">
          <label for="username">Username:</label><br>
          <input type="text" id="username" name="username" value="<%= teacher.username %>" required><br><br>

          <label for="Fullname">Fullname:</label><br>
          <input type="text" id="Fullname" name="Fullname" value="<%= teacher.Fullname %>" required><br><br>

          <label for="email">Email:</label><br>
          <input type="email" id="email" name="email" value="<%= teacher.email %>" required><br><br>

          <label for="password">Password:</label><br>
          <input type="password" name="password" placeholder="Enter new password" value="<%= teacher.password %>"
            required><br><br>

          <label for="dob">Date of Birth:</label><br>
          <input type="date" id="dob" name="dob" value="<%= teacher.dob %>" required><br><br>

          <label for="address">Address:</label><br>
          <input type="text" id="address" name="address" value="<%= teacher.address %>" required><br><br>

          <label for="contact">Contact:</label><br>
          <input type="tel" id="contact" name="contact" pattern="[0-9]{8}" value="<%= teacher.contact %>"
            required><br><br>

          <label for="subject">Subject:</label><br>
          <select id="subject" name="subject" class="form-control" value=<%=teacher.subject %> required>
            <option value="English" <%=(teacher && teacher.subject==='English' ) ? 'selected' : '' %>>English</option>
            <option value="Math" <%=(teacher && teacher.subject==='Math' ) ? 'selected' : '' %>>Math</option>
            <option value="Science" <%=(teacher && teacher.subject==='Science' ) ? 'selected' : '' %>>Science</option>
          </select><br><br>

          <label for="teachingcert">Teaching Certificate(.pdf):</label><br>
          <input type="file" id="teachingcert" name="teachingcert" class="form-control" accept="application/pdf"
            value=<%=teacher.teachingcert %> required><br><br>

          <label for="grade">Teaching Grade:</label><br>
          <select id="grade" name="grade" class="form-control" value=<%=teacher.teacherGrade %> required>
            <option value="N-level" <%=(teacher && teacher.teacherGrade==='N-level' ) ? 'selected' : '' %>>N-level
            </option>
            <option value="O-level" <%=(teacher && teacher.teacherGrade==='O-level' ) ? 'selected' : '' %>>O-level
            </option>
          </select><br><br>

          <label for="resume">Resume(.pdf):</label><br>
          <input type="file" id="resume" name="resume" class="form-control" accept="application/pdf"
            value=<%=teacher.resume %> required><br><br>

          <label for="image">Current Image:</label><br>
          <input type="file" name="currentImage" value=<%=teacher.image %> readonly><br>
          <img src="/images/<%= teacher.image %>" width="20%"><br><br>

          New image:<br> <input type="file" id="image" name="image" accept="image/*" onchange="checkSize(this)"
            value=<%=teacher.image %>><br><br>

          <button type="submit">Update Teacher</button>
        </form>
        <script>
          function checkSize(input) {
            if (input.files[0].size > 800000) { // 800 KB in bytes
              alert("Image must be smaller than 800KB");
              input.value = "";
            }
          }
        </script>
        <% } else { %>
          <p>No teacher found.</p>
          <% } %>
            <a href="/TeacherList">Back</a>
    </div>
  </div>
</body>

</html>
