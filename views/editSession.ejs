<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Tuition Centre App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Tuition Centre App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addSession">Add new session</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>
<div class="container">
        <br>
        <div class="text-center"><h2>Edit Session Info</h2></div>
        <br>
        <div class="text-center">
          <% if (session) %> 
            <form action="/editSession/<%= session.sessionId %>" method="POST" enctype="multipart/form-data">
                <label for="subject">Subject:</label><br>
                <input type="text" id="subject" name="subject" value = "<%= session.subject %>" required><br><br>

                <label for="session_date">Date:</label><br>
                <input type="date" id="session_date" name="session_date" value = "<%= session.session_date %>" required><br><br>

                <label for="session_time">Time:</label><br>
                <input type="time" id="session_time" name="session_time" value = "<%= session.session_time %>" required><br><br>

                <label for="teacher_name">Appoint Teacher:</label><br>
                <input type="text" id="teacher_name" name="teacher_name" value = "<%= session.teacher_name %>" required><br><br>

                <label for="duration">Duration:</label><br>
                <input type="time" id="duration" name="duration" value = "<%= session.duration %>" required><br><br>

                <label for="max_students">No. of students:</label><br>
                <input type="number" id="max_students" name="max_students" value = "<%= session.max_students %>" required><br><br>

                <button type="submit">Update Session</button>
            </form>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>