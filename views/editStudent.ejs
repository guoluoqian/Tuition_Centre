<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Tuition Centre App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Tuition Centre App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addStudent">Add new student</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Edit Student Info</h2></div>
        <br>
        <div class="text-center">
          <% if (student) { %> 
            <form action="/editStudent/<%= student.studentId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Student Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= student.name %>" required><br><br>

                <label for="dob">Date of Birth:</label><br>
                <input type="date" id="dob" name="dob" value = <%= student.dob %> required><br><br>

                <label for="grade">Student Grade:</label><br>
                <input type="text" id="grade" name="grade" value = "<%= student.grade %>" required><br><br>

                <label for="contact">Contact:</label><br>
                <input type="tel" id="contact" name="contact" pattern="[0-9]{8}" value = <%= student.contact %> required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="file" name="currentImage" value = <%= student.image %> readonly><br>
                <img src = "/images/<%= student.image %>" width="20%"><br><br>

                New image:<br> <input type="file" id="image" name="image" accept="image/*" onchange="checkSize(this)" value = <%= student.image %>><br><br>

                <button type="submit">Update Student</button>
            </form>
            <script>
            function checkSize(input) {
              if (input.files[0].size > 800000) { // 800 KB in bytes
                alert("Image must be smaller than 800KB");
                input.value = "";
                }}
            </script>
          <% } else { %>
              <p>No student found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
